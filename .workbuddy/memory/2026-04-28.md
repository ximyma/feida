# 2026-04-28 工作记录

## 考勤模块全面完善（真实数据库化）

### 修复内容

**1. 考勤打卡（AttendancePage.tsx）**
- 打卡功能从模拟演示改为真实 API：`POST /api/attendance/clock-in` / `clock-out`
- 打卡状态从 localStorage 读取登录用户的 employeeId，页面加载时从数据库拉取当日打卡记录
- 修复 totalEmployees 硬编码=48 → 改为从数据库 employees 表动态获取在职人数
- 打卡界面显示迟到分钟数、班次名称、实时工时

**2. 考勤规则页面（AttendanceRulesPage.tsx）**
- 新增「设备对接」Tab（共5个Tab：班次、排班、年假、免考勤、设备）
- 年假规则从模拟保存改为真实存入 `leave_rule_configs` 表
- 设备对接支持：中控ZKTime / 钉钉 / 企业微信 / 飞书 / APP内置
  - 每个设备可配置API密钥（存入 `attendance_devices` 表）
  - 测试连接（调用对应平台的 token 接口验证）
  - 同步数据（钉钉/企业微信实现真实打卡记录拉取；中控/飞书给出接入说明）
  - 配置摘要展示、操作反馈提示

**3. 后端新增 API（standalone.ts）**
- `GET /api/attendance/devices` - 获取设备列表（自动初始化5条默认设备）
- `PUT /api/attendance/devices/:id` - 保存设备配置
- `POST /api/attendance/devices/:id/test` - 测试连接
- `POST /api/attendance/devices/:id/sync` - 同步打卡数据（钉钉/企业微信实现真实 HTTP 调用）
- `POST /api/attendance/import` - 批量导入打卡记录
- `GET /api/attendance/leave-rules` - 年假规则读取
- `PUT /api/attendance/leave-rules` - 年假规则保存

**4. 数据库新增表（database.service.ts + 已迁移到数据库）**
- `attendance_devices`（考勤设备配置）
- `leave_rule_configs`（年假规则全局配置）

### 测试状态
- 构建成功（24s），服务器启动正常（3000端口）
- `/api/attendance/devices` 返回5条设备数据 ✅
- `/api/attendance/leave-rules` 返回默认规则 ✅

---

## 考勤规则页面二次修复（班次/排班/年假余额真实可用）

### 问题修复

**1. 后端新增 `/api/attendance/leave-balances`**
- JOIN employees 表，返回 employeeName、department、empNo
- 支持 leaveType / year 过滤参数
- 49 条年假余额记录，含员工姓名 ✅

**2. 后端新增 `GET /api/schedules` 专属路由**
- 替换通用 CRUD 路由对排班的处理
- 无日期参数时默认返回前14天至后16天（约30天窗口）数据，最多500条
- 解决之前今日无数据显示空列表问题，211 条排班记录返回正常 ✅
- 支持 date / employeeId / department / startDate / endDate 过滤

**3. 前端 AttendanceRulesPage.tsx 修复**
- 年假余额表格：调用新接口，显示 employeeName+department，剩余天数用 availableDays 字段
- 排班管理：默认日期为空（显示全部），加「清除日期」按钮，前端按日期+部门双重过滤
- 班次 isActive checkbox：修正为 `!!formData.isActive && formData.isActive !== 0`，兼容数字0/1和boolean

### 验证
- 构建成功（exitCode:0）✅
- leave-balances API 返回 49 条含姓名数据 ✅
- schedules API 无日期时返回 211 条 ✅

