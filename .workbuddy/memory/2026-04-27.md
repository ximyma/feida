# 2026-04-27 工作记录

## 全量检查 Mock 数据 → 真实数据库迁移状态

- 对 `d:/feida/client/src` 进行全面扫描
- 结论：**64个业务页面全部已接真实 `/api/*` 接口**，后端为 SQLite（`server/standalone.ts`）
- 唯一遗留：`store/useHRData.ts`（1271行），包含 `generateMockEmployees()` 等 Mock 生成器，但**没有任何业务页面调用**，是历史孤岛，可安全删除
- `api/index.ts` 是空壳，所有注释，业务页面均直接使用 `fetch()`

## 修复：SelfServicePage 硬编码用户 ID

- 文件：`client/src/pages/SelfServicePage/SelfServicePage.tsx`
- 问题：`const CURRENT_USER_ID = 'emp-1'` 硬编码
- 修复：替换为 `getCurrentUserId()` 函数，从 `localStorage.getItem('user')` 读取登录用户的 `employeeId` 或 `id`
